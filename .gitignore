# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.sqlite3

# uv
uv.lock

# Node / Angular
node_modules/
frontend/dist/
.angular/

# IBKR Client Portal Gateway distribution (user-supplied, not redistributed).
# The directory itself is tracked via infra/ibkr-gateway/dist/.gitignore so
# the Dockerfile has a target for COPY; the .zip inside is ignored.
infra/ibkr-gateway/dist/*
!infra/ibkr-gateway/dist/.gitignore
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env / secrets
.env
.env.*
!.env.example

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Docker
*.pid

# Local overrides
infra/docker-compose.override.yml

# Django media (filing full-text dumps written here in dev)
backend/media/

# Project-local notes (dev creds, scratch). Never commit.
MEMORY.md

# pnpm content-addressable cache (auto-restored from pnpm-lock.yaml)
frontend/.pnpm-store/
.tmp-screenshots/

# Scratch verification screenshots / artifacts (not part of the repo)
dev-artifacts/

# Claude Code per-session scheduling lock
.claude/scheduled_tasks.lock
